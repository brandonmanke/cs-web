node_modules/
dist/
build/
client/src/generated/
.codex

# The repo has no binary assets and no assets/ directory. If you ever drop
# reference material somewhere to look at it, it does not belong in a commit.
assets/
